<div class="container" style = "margin-top: 20px">
  <div style="display: flex">
    <%= gravatar_for @user %>
  <div style="display: inline-block;">
  <h2>@<%= @user.username %></h2>
  <h3><%=@user.email %></h3>
  </div>
  </div>
  <br><br>
<div style="display: flex">
   <% if logged_in? && current_user = @user%>

      <%= link_to 'Edit your account', edit_user_path(@user), class: "btn btn-secondary" , style: "margin-right: 10px;"%>
      <%= button_to  'Delete your account', user_path(@user), method: :delete, data: { confirmation: "Are you sure?"}, class: "btn btn-danger"%>

  <% end %>

</div>
  <% if @user.admin?  %>
    <i><small>You are an admin</small></i>
  <% end %>
</div>

<hr>
<h3 align="center" style="margin-bottom: 20px; margin-top: 10px;">Posts</h3>
<%= will_paginate @user_articles , class: 'pagination'%>
<%= render 'articles/article', obj: @user_articles %>
<%= will_paginate @user_articles , class: 'pagination'%>

